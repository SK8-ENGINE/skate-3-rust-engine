{
  "id": "examples.game-of-skate",
  "api": 2,
  "name": "Game of SKATE",
  "version": "0.3.0",
  "author": "Skate3clone",
  "description": "Host-first sequential turns from a shared start. Waiting players are suspended and watch the active player's camera. Match the trick or take a letter; last player without SKATE wins.",
  "entry": "main.lua",
  "settings": {
    "copy_seconds": {
      "type": "number",
      "label": "Copy window (seconds)",
      "description": "How long copiers have to match the set trick before taking a letter.",
      "default": 45,
      "min": 15,
      "max": 120,
      "step": 5
    },
    "allow_ollie": {
      "type": "boolean",
      "label": "Allow ollie as a set",
      "description": "If off, ollies and nollies cannot start a round.",
      "default": false
    }
  }
}
