[package]
name = "skate-steam-relay"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "skate-steam-relay"
path = "src/main.rs"

[dependencies]
steamworks = { version = "=0.13.1", features = ["raw-bindings"] }
skate-net = { path = "../skate-net" }
